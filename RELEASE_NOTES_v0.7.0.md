# 산보 0.7.0 — 더 오래 기록하고, 더 안전하게 보관하고, 더 생생하게 돌아보기

산보 0.7.0은 배터리 효율과 장시간 기록 안전성, 기록 보존, 산책 회고 경험을 함께 다듬은 정식 릴리스입니다.

## 주요 변화

### 배터리와 장시간 기록

- GPS 수집을 절전(20초)·균형(8초)·정밀(4초) 모드로 나누고 거리 필터도 각각 조정했습니다.
- 화면이 백그라운드로 가면 위치 수집은 유지하되 불필요한 1초 UI 갱신과 매 샘플 화면 상태 발행을 멈춥니다.
- 20분간 한 장소에 머물면 확인 알림을 보내고, 30분간 움직임이 없으면 기록을 안전하게 저장한 뒤 자동 종료합니다.
- 시작 후 2시간 45분에 종료 예고를 보내며, 3시간이 되면 기록을 저장하고 자동 종료합니다.
- CPU WakeLock은 정밀 모드에서만 사용합니다.

### 지도와 산책 회고

- 지도와 타임라인을 연결해 약 20초 동안 경로를 재생할 수 있습니다.
- 슬라이더로 특정 시점의 위치·활동·장소를 확인하고 선택한 구간을 지도에서 강조할 수 있습니다.
- 자주 머무는 장소에 직접 이름을 붙이고, 가까운 위치에서 로컬 장소 이름을 재사용할 수 있습니다.
- 사용자가 선택한 경우에만 기기의 주소 제안을 불러옵니다.

### 데이터 보존과 백업

- 완료한 산책, 원본 GPS, 분 윈도우, 사용자 수정, 메모, 장소를 하나의 `.sanbo` 파일로 내보내고 가져올 수 있습니다.
- 가져오기는 기존 기록에 병합되며 같은 세션 ID는 건너뜁니다.
- 파일·행·좌표·버전·DB 무결성을 먼저 검증하고, 오류가 나면 전체 가져오기를 되돌립니다.
- DB 스키마를 v2로 올리면서 기존 v1 기록이 유지되는 마이그레이션을 추가했습니다.

### 안정성과 사용성

- 진행 중 기록의 체크포인트·자동 종료 검사를 하나의 30초 유지보수 경로로 통합했습니다.
- 권한 거부, 비정상 종료 복구, 빈 기록, 상세 삭제, 설정 안전 안내 흐름을 다시 점검했습니다.
- iOS 백그라운드 위치 모드와 위치 사용 표시 설정을 보강했습니다.
- Android 릴리스 키 설정을 저장소 밖으로 분리하고, 키가 없으면 릴리스 빌드가 명확히 실패하도록 했습니다.

## 업데이트 안내

- v0.3.2 배포본과 같은 서명 인증서를 사용하므로 기존 앱 위에 v0.7.0을 설치할 수 있습니다.
- 정상적인 앱 업데이트는 기존 산책 기록을 유지하며, 첫 실행 때 DB v1→v2 마이그레이션이 자동 적용됩니다.
- 중요한 기록은 업데이트 전에 설정에서 전체 백업을 만들어 별도로 보관하는 것을 권장합니다.
- 앱을 삭제하면 로컬 데이터도 삭제됩니다. 정밀 위치 보호를 위해 OS 자동 클라우드 백업은 사용하지 않습니다.

## 개인정보 보호 주의

`.sanbo` 백업은 정확한 GPS 좌표를 포함하는 암호화되지 않은 JSON 파일입니다. 신뢰할 수 있는 저장소에만 보관하고 다른 사람에게 그대로 공유하지 마세요.

## Android 설치 파일

기기 CPU에 맞는 APK 하나만 설치하세요.

| 파일 | 대상 | SHA-256 |
|---|---|---|
| `app-arm64-v8a-release.apk` | 대부분의 최근 Android 기기 | `9b981c044698af55d13fd2d66336e9a047baa482f59f6fd6f40fb751eeff6322` |
| `app-armeabi-v7a-release.apk` | 구형 32비트 Android 기기 | `39d0713251882e083feb836c773ddd3f54ff2951178abd559c722b9e0571e0c4` |
| `app-x86_64-release.apk` | x86_64 에뮬레이터·일부 기기 | `4b88d637672a55d5af22e813967db4ff75132521a65915d32228d209618a214c` |

## 검증

- `flutter analyze` 통과
- 전체 Flutter 테스트 114개 통과
- Android `0.7.0+10` ABI별 릴리스 APK 빌드 및 서명 검증
- Flutter의 ABI별 manifest versionCode(`1010`/`2010`/`4010`) 확인
- 기존 v0.3.2 배포 APK와 서명 인증서 일치 확인
- iOS Simulator debug 빌드 통과
- DB v1→v2 기존 행 보존, 백업 왕복·중복 병합·손상 입력 rollback 검증

## 알려진 제한

- 실제 배터리 소모는 기기, GPS 신호, 제조사 절전 정책에 따라 달라집니다. Galaxy 실기기 장시간 측정은 계속 필요합니다.
- 백업 파일 암호화, 자동 예약 백업, 50MB 초과 스트리밍 백업은 아직 지원하지 않습니다.
- Android 제조사별 장시간 백그라운드 수집과 iOS 잠금 화면 수집은 실기기 추가 검증이 필요합니다.
- iOS 배포 전에는 background location 사용 사유와 권한 안내 문구에 대한 App Store 심사가 필요합니다.
