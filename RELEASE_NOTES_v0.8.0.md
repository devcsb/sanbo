# 산보 0.8.0 — 고속 이동 확인과 기록 구간 정리

산보 0.8.0은 산책이 아닌 이동이 기록에 섞이는 문제를 앞뒤로 다룬 정식 릴리스입니다.
기록 중에 차량 속도로 움직이면 종료 여부를 먼저 확인하고, 이미 저장된 기록에서는
해당 구간만 골라 산책에서 빼거나 되돌릴 수 있습니다. 위치 스트림과 알림 복구
경로도 함께 손봤습니다.

## 주요 변화

### 고속 이동 종료 확인

- 최근 120초 안에서 신뢰 가능한 28.8km/h 이상 이동이 누적 60초가 되면 자동으로 종료하지 않고 종료 여부를 확인합니다.
- `기록 종료`와 `계속 기록` 중에서 고를 수 있고, 14.4km/h 이하 이동이 연속 30초 이어지기 전에는 같은 이동으로 다시 알리지 않습니다.
- 앱이 배경에 있거나 화면이 꺼져 있으면 알림으로 알리고, 알림을 탭하면 홈의 고속 경고로 이동합니다. 앱이 종료된 상태에서 탭해도 복구 뒤 같은 경고를 다시 구성합니다.
- 알림 권한을 거부하거나 알림 API가 실패해도 세션 생성·위치 수집·고속 판단은 그대로 동작합니다.
- 세션 경고를 종류별로 구분해 표시하며, 복구된 세션에 남아 있던 고속 알림은 정리합니다.

### 완료 기록의 구간 제외와 복원

- 완료된 산책에서 연속 구간을 골라 `산책에서 제외`할 수 있고, 제외된 구간은 `제외 취소`로 되돌립니다.
- 차를 타고 이동한 구간처럼 산책이 아닌 부분을 빼면 지도, 세션 통계, 기록 화면 합계, 일별 합계를 함께 새로 읽습니다.
- 원본 GPS 샘플은 지우지 않으므로 언제든 원래 계산으로 복원할 수 있습니다. 모든 점을 제외해도 세션과 원시 샘플은 남고 경로·집계만 0으로 갱신됩니다.
- 제외 상태는 앱을 재시작해도 유지되며, 전체 백업·복원과 내보내기에도 함께 담깁니다.

### 기록 안정성

- 기록 중 위치 서비스를 끄거나 권한을 철회하면 `미완료 기록` 복구 카드로 안내하고, 다시 허용하면 `이어서 기록`으로 복귀합니다.
- Android 위치 공급자가 끊긴 뒤 되살아나는 상황과, 오래된 GPS 기준점 때문에 차량 이동 구간이 끊겨 보이던 문제를 고쳤습니다.
- 산책 시각을 UTC로 저장해 기기 타임존이 바뀌어도 기록 시각이 흔들리지 않습니다. 알 수 없는 타임존은 기기 기준 시각을 보존합니다.
- 쓸 만한 GPS 고정을 하나도 받지 못한 기록은 저장하지 않고, 걸러진 샘플은 점검용으로 남깁니다.
- 앱 복귀와 콜드 스타트에서 알림 준비 상태를 다시 확인해 경고 알림이 누락되던 경로를 막았습니다.
- iOS 알림 채널 등록과 알림 탭 처리를 scene delegate 경로로 정리하고, 위치 시작 실패 시 대체 경로를 보강했습니다.

## 업데이트 안내

- 버전 `0.8.0+13`이며, 0.7.x와 동일한 application ID·서명 인증서를 사용합니다.
- DB 스키마가 `4`로 올라갑니다. 업데이트하면 자동 마이그레이션되고 기존 산책 기록·GPS·수정·메모·장소는 유지됩니다. 이전 버전으로 되돌리는 downgrade는 지원하지 않습니다.
- 전체 백업 출력은 `backup_schema_version: 2`이며 기존 v1 백업도 그대로 읽습니다. v1을 읽으면 구간 제외는 빈 목록으로 정규화됩니다.
- 앱을 삭제하면 로컬 기록도 삭제되므로 중요한 기록은 업데이트 전에 전체 백업을 권장합니다.
- 기존 0.7.2 APK 위에는 `adb install -r` 또는 일반적인 Android 업데이트 방식으로 설치할 수 있습니다.

## Android 설치 파일

기기 CPU에 맞는 APK 하나만 설치하세요.

| 파일 | 대상 | versionCode | SHA-256 |
|---|---|---:|---|
| `app-arm64-v8a-release.apk` | 최근 64비트 Android | `2013` | `f8a1cfe12c3cd5ef8ee30c8ccdcaeb0c12a2f351071301695f3e452f42cb9705` |
| `app-armeabi-v7a-release.apk` | 구형 32비트 Android | `1013` | `02a9687a7f774024f4f3e0eafb48d81a1cbcad8f6b2389a97d746ca42b13b1a4` |
| `app-x86_64-release.apk` | x86_64 에뮬레이터·일부 기기 | `4013` | `26f4cd82146e2fc22c8ed3aea754e5cc2a0bd0810633721444ece25629d4b76d` |

## 검증

- `python3 scripts/verify_prd_trd.py` 통과
- `flutter analyze` 문제 없음
- Flutter 전체 테스트 356개 통과
- 서명된 3개 ABI release APK 빌드 통과
- 3개 ABI APK의 manifest 권한·위치 FGS 타입 확인
- 3개 ABI APK의 서명 인증서 SHA-256이 기존 공개 릴리스와 일치

## 알려진 제한

- 고속 이동 알림·구간 제외의 물리 기기 검증은 아직 미판정입니다. emulator와 simulator 증거만 있으며 `docs/DEVICE_VALIDATION.md` 절차로 후속 측정이 필요합니다.
- Galaxy 실기기에서 잠금 화면·제조사 절전·알림 전달과 배터리 수치는 현장에서 확인하지 못했습니다.
- 배터리 소모는 추적 모드·GPS 신호·기기 제조사 정책에 따라 달라집니다.
- 백업은 정밀 위치를 포함하는 암호화되지 않은 JSON이며, 50MB 초과 스트리밍 백업은 지원하지 않습니다.
- iOS background location은 실험 지원 상태이며 App Store 제출 전 별도 검증이 필요합니다.
