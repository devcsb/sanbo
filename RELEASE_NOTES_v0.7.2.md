# 산보 0.7.2 — 일별 운동량과 장시간 기록 안정화

산보 0.7.2는 최근 7일 운동량을 한눈에 돌아보는 기능을 추가하고, 장시간 GPS
기록에서 발생할 수 있는 거리 과대 계산·속도 0 표시 문제를 줄인 정식 릴리스입니다.
잊고 켜 둔 기록의 전체 자동 종료 한도도 5시간으로 조정했습니다.

## 주요 변화

### 일별 운동량

- 기록 화면에서 최근 7일을 날짜별로 탐색할 수 있습니다.
- 날짜별 총 거리·운동 시간·산책 횟수를 표시합니다.
- 빈 날짜는 0값으로 표시하고, 날짜를 선택해도 최근 기록 목록의 흐름은 유지합니다.
- 걸음 수·칼로리·Samsung Health/Health Connect 연동은 이번 범위에 포함하지 않습니다.

### 장시간 GPS 기록 안정화

- 60초를 넘는 GPS 공백 양끝을 직선으로 연결하지 않아 거리 과대 계산을 줄였습니다.
- Android 위치 공급자가 이동 중 `speed=0`을 보고해도 인접 GPS 좌표로 속도를 계산합니다.
- 필터된 GPS 점프의 공급자 속도가 화면 속도를 덮어쓰지 않도록 했습니다.
- 실시간·복구·종료 집계가 같은 GPS 공백 규칙을 사용합니다.

### 세션 보호 정책

- 전체 기록은 4시간 45분에 종료 예고를 표시하고, 5시간에 자동 저장·종료합니다.
- 한 장소 정지 20분 경고·30분 자동 저장·종료 정책은 유지합니다.
- 자동 저장 실패 시 기존처럼 복구 가능한 상태와 재시도 안내를 유지합니다.

## 업데이트 안내

- 버전 `0.7.2+12`이며, 0.7.1과 동일한 application ID·서명 인증서를 사용합니다.
- 정상적인 앱 업데이트는 기존 산책 기록·GPS·수정·메모·장소를 유지합니다.
- 앱을 삭제하면 로컬 기록도 삭제되므로 중요한 기록은 업데이트 전에 전체 백업을 권장합니다.
- 기존 0.7.1 APK 위에는 `adb install -r` 또는 일반적인 Android 업데이트 방식으로 설치할 수 있습니다.

## Android 설치 파일

기기 CPU에 맞는 APK 하나만 설치하세요.

| 파일 | 대상 | versionCode | SHA-256 |
|---|---|---:|---|
| `app-arm64-v8a-release.apk` | 최근 64비트 Android | `2012` | `e66cca19659f9489d89d1a42e96abe20d2b5ccf036892dac2bd52e90f2cc1b4e` |
| `app-armeabi-v7a-release.apk` | 구형 32비트 Android | `1012` | `8243917093142f2e9d4bd55763fea9ca972553bac431861fae24f2589640b3ef` |
| `app-x86_64-release.apk` | x86_64 에뮬레이터·일부 기기 | `4012` | `2b20f843c5a7ae7f9fc43eded19a0fa6dd8990e79daa3f80d4262cc5e13c1182` |

## 검증

- `python3 scripts/verify_prd_trd.py` 통과
- `flutter analyze --no-pub` 문제 없음
- Flutter 전체 테스트 154개 통과
- `bash scripts/run_quality_loop.sh --release-apk` 통과
- 3개 ABI APK의 manifest 권한·위치 FGS 타입 확인
- 3개 ABI APK의 서명 인증서 SHA-256이 기존 공개 릴리스와 일치

## 알려진 제한

- Galaxy 실기기에서 5시간 잠금 화면·제조사 절전·알림 전달을 실제 측정하지 못했습니다.
- 배터리 소모는 추적 모드·GPS 신호·기기 제조사 정책에 따라 달라집니다.
- 백업은 정밀 위치를 포함하는 암호화되지 않은 JSON이며, 50MB 초과 스트리밍 백업은 지원하지 않습니다.
- iOS background location은 실험 지원 상태이며 App Store 제출 전 별도 검증이 필요합니다.
