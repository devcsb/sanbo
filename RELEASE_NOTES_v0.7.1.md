# 산보 0.7.1 — 장기 기록과 업데이트를 더 안전하게

산보 0.7.1은 0.7.0의 기능을 유지하면서 장시간 GPS 기록의 저장 내구성, 통계 정확도,
대용량 백업 응답성, 기록 목록 성능과 릴리스 검증을 강화한 안정화 릴리스입니다.

## 주요 변화

### 기록 유실·오염 방어

- 체크포인트 쓰기와 종료·삭제를 직렬화하고 늦게 도착한 callback이 종료된 세션을 되살리지 못하도록 세대 경계를 추가했습니다.
- 저장 종료에 실패한 뒤 기록을 재개해도 메모리에만 남은 GPS 샘플을 다시 체크포인트 대상으로 복구합니다.
- NaN·무한대·범위 밖 GPS 좌표와 정확도·속도·고도 메타데이터가 거리, DB, 백업을 오염시키지 않도록 정규화합니다.
- 저장소 복구 조회 실패와 저장 재시도를 신규 산책 시작과 분리해 사용자가 안전하게 다시 시도할 수 있습니다.

### 정확도와 성능

- 첫 GPS 수신 전, 마지막 수신 후, 60초를 넘는 GPS 공백은 이동 시간으로 간주하지 않습니다.
- 긴 GPS 공백 양끝을 직선거리로 연결하지 않아 거리 과대 계산을 줄였습니다.
- 기록 목록은 최초 50개씩 확장하고 전체 통계는 SQLite 집계로 계산합니다.
- 완료 기록 조회 인덱스와 안정적인 동률 정렬을 추가하고 실제 v2→v3 데이터 보존을 검증했습니다.
- 상세 화면의 독립적인 샘플·윈도우 조회를 병렬화했습니다.

### 백업·프라이버시·릴리스 안전성

- `.sanbo` 백업은 한 번만 백그라운드 isolate에서 파싱한 뒤 같은 검증 결과를 가져오기에 재사용합니다.
- 설정에 CARTO 지도 타일 조회 시 대략적인 지도 영역과 네트워크 정보가 외부로 전달될 수 있음을 명시했습니다.
- 품질 스크립트는 필수 manifest 도구가 없거나, 공개 릴리스와 인증서가 다르거나, 명령 실행 후 작업 트리가 변하면 실패합니다.
- 버전을 `0.7.1+11`로 올려 기존 `0.7.0+10` 설치 위에 업데이트할 수 있습니다.

## 업데이트 안내

- application ID와 서명 인증서는 0.7.0 공개 APK와 동일합니다.
- DB schema v2는 시작 시 v3로 증가형 마이그레이션되며 산책·GPS·구간 수정·메모·장소를 유지합니다.
- 정상 업데이트는 앱 데이터 영역을 유지합니다. 앱을 삭제하면 로컬 기록도 삭제되므로 중요한 기록은 먼저 전체 백업으로 보관하세요.
- Android 자동 클라우드·기기 이전 백업은 정밀 위치 보호를 위해 계속 비활성화되어 있습니다.

## Android 설치 파일

대부분의 최근 Android 기기는 `app-arm64-v8a-release.apk`를 설치하면 됩니다.

| 파일 | 대상 | SHA-256 |
|---|---|---|
| `app-arm64-v8a-release.apk` | 최근 64비트 Android | `1f41c6bc2b461be26f1fac34edb4edcb2be42c04f2f1c58a8ac47cb2332d0b6d` |
| `app-armeabi-v7a-release.apk` | 구형 32비트 Android | `901e1d05cfd9bc02fc197866a4bbce475c1674392ce374ca32b54dd7e60c31f0` |
| `app-x86_64-release.apk` | x86_64 에뮬레이터·일부 기기 | `8db5073264040329f8336c726f4180e2fffb3b42fb1252b75b1e39cfb9bef3af` |

## 검증

- PRD/TRD 구조 검증 통과
- `flutter analyze`: 문제 없음
- Flutter 테스트 133개 통과
- Android ABI별 release APK 빌드 성공
- manifest의 위치·FGS location·알림·WakeLock 권한과 위치 서비스 타입 확인
- 세 APK의 서명 인증서 SHA-256이 기존 공개 릴리스와 동일함을 확인
- versionName `0.7.1`, ABI별 versionCode `1011` / `2011` / `4011` 확인
- 현실적인 v2 DB fixture의 v3 마이그레이션에서 산책·GPS·수정·메모·장소 보존 확인

## 알려진 제한

- Galaxy 실기기 배터리·제조사별 백그라운드·잠금 화면 장시간 측정은 절차만 고정되어 있고 이번 환경에서는 실행하지 못했습니다.
- 실제 기기에서 0.7.0 APK 위에 `adb install -r`하는 설치 검증은 연결된 Android 기기가 없어 인증서·versionCode 비교로 대체했습니다.
- 백업은 정밀 위치를 포함하는 암호화되지 않은 JSON이며 50MB를 초과하는 스트리밍 백업은 아직 지원하지 않습니다.
- 지도 타일을 열면 CARTO에 타일 좌표와 IP 등 네트워크 정보가 전달될 수 있습니다.
- iOS background location은 실험 지원 상태이며 App Store 제출 전 실기기·권한 문구·심사 검증이 필요합니다.
